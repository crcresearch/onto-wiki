name: test-feature

# Minimal workflow file for the test-feature fixture. install_feature
# copies it to .github/workflows/ during install; uninstall_feature
# removes it. Not designed to actually run in CI.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "test-feature workflow placeholder"
