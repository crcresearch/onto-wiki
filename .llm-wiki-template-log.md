# Template sync log

Tracks this project's relationship to the upstream `crcresearch/llm-wiki-memory-template`.

## [2026-06-23] diverged | operating model → agent layer

onto-wiki was created from the template via "Use this template" (no shared git
history; baseline = template state at instantiation, 2026-06-23).

**Divergence**: the wiki's operating model (page formats, page-type catalog,
typed edges, source-first review-gated ingest pipeline, figure review, KG
topology-vs-content, commit/log discipline) was moved OUT of the per-wiki
`SCHEMA_<repo>.md` and into a single static, agent-agnostic doc
`wiki/agents/memory-architecture.md`. The SCHEMA is now a thin domain pointer.
This separates *wiki-as-agentic-memory* (operating system) from *linked-data
domain knowledge* (content), and supports multiple harnesses by reference.

**Diverged files** (dropped from `update-from-template.sh` sync lists so a
future upstream pull won't clobber them):

- `wiki/init-wiki.sh` — authors thin SCHEMA + leaves mechanics to memory-architecture.md (removed the old dual-maintenance heredoc/append pair)
- `.claude/skills/wiki-{source,experiment,lint}.md` — repointed to memory-architecture.md; wiki-source rewritten to the source-first pipeline
- `.claude/commands/wiki-{source,experiment,lint}.md` — repointed
- `wiki/agents/claude-code/templates/{claude-md-snippet.md,memory-seed.md,session-start-hook.sh}` — repointed

**New files** (not in upstream; not synced):

- `wiki/agents/memory-architecture.md` — the agent-agnostic operating model
- `wiki/Edge-Types.md.template` — extended with `usesTool`/`reuses`/`governedBy` (not in sync lists upstream)

**Still tracking upstream** (unchanged, remain in sync): `llm-wiki.md`,
`wiki/agents/{README,discipline-gates,verification-gate,wiki-write-protocol}.md`,
`scripts/{update-from-template,check-template-version}.sh`, the wiki-write-protocol
scripts, feature infra, `.gitignore`, and the claude-code overlay `setup.sh`/`README.md`.

**To upstream later**: diff the diverged files against current
`llm-wiki-memory-template`, generalize `memory-architecture.md` beyond onto-wiki,
PR, then restore the files to the sync lists once upstream == local.
